---
title: 'First clip'
description: 'A placeholder entry showing how a video post is put together.'
pubDate: 'Sep 16 2026'
src: ['/videos/first-clip.webm', '/videos/first-clip.mp4']
poster: '/blog-placeholder-2.jpg'
aspect: '16 / 9'
---

Drop the encoded files in `public/videos/` and point `src` at them. Listing more than
one file lets the browser pick the first format it supports, so put the smallest
encoding — usually WebM — first.

Everything below the player is ordinary Markdown, exactly like a blog post.
